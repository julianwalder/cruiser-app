name: Deploy to Staging

on:
  push:
    branches: [dev, staging]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build project
        run: |
          cd packages/frontend
          npm run build
          
      - name: Deploy to Cloudflare Workers Staging
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy --env staging
          workingDirectory: .
          
      - name: Upload KV data for staging
        run: |
          cd packages/frontend
          node scripts/upload-kv.js
          cd ../..
          npx wrangler kv:bulk put packages/frontend/kv-data.json --binding=STATIC_CONTENT_STAGING --env staging
          
      - name: Notify deployment
        run: |
          echo "✅ Successfully deployed to staging environment!"
          echo "🌍 Staging URL: https://cruiser-aviation-frontend-staging.julian-pad.workers.dev"
          echo "📊 Deployment completed at $(date)" 